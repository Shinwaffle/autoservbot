[Unit]
Description=autoservbot discord bot
After=network.target

[Service]
Type=simple
User=shin
WorkingDirectory=/home/shin/autoservbot
ExecStart=/usr/bin/python3 main.py
Restart=always

[Install]
WantedBy=default.target
